name: Auditoria completa da SU Mega Beta

on:
  push:
    branches: [beta]
    paths-ignore:
      - "qa/**"
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

concurrency:
  group: qa-su-mega-beta
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Baixar Beta
        uses: actions/checkout@v4
        with:
          ref: beta

      - name: Preparar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Instalar Playwright e Chromium
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium

      - name: Validar sintaxe de todos os JavaScripts
        run: |
          find . -type f -name '*.js' -not -path './node_modules/*' -print0 | xargs -0 -n1 node --check
          node --check tests/qa-smoke.mjs

      - name: Iniciar aplicativo local
        run: |
          nohup python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/su-mega-qa-server.log 2>&1 &
          for attempt in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/ >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/su-mega-qa-server.log
          exit 1

      - name: Testar todas as áreas e fluxos principais
        id: qa
        continue-on-error: true
        env:
          QA_APP: mega
          QA_VERSION: v27
          QA_BASE_URL: http://127.0.0.1:4173/
        run: node tests/qa-smoke.mjs

      - name: Guardar relatório como artefato
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: auditoria-su-mega-beta
          path: qa/
          if-no-files-found: warn
          retention-days: 30

      - name: Publicar resultado no commit
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TARGET_URL="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          if test -f qa/latest-report.json; then
            PASSED=$(jq -r '.totals.passed' qa/latest-report.json)
            FAILED=$(jq -r '.totals.failed' qa/latest-report.json)
            TOTAL=$(jq -r '.totals.total' qa/latest-report.json)
            if [ "$FAILED" = "0" ]; then STATE="success"; else STATE="failure"; fi
            DESCRIPTION="$PASSED/$TOTAL testes aprovados; $FAILED falhas"
          else
            STATE="error"
            DESCRIPTION="A auditoria terminou antes de gerar o relatório"
          fi

          jq -n \
            --arg state "$STATE" \
            --arg context "qa/auditoria-completa" \
            --arg description "$DESCRIPTION" \
            --arg target_url "$TARGET_URL" \
            '{state:$state,context:$context,description:$description,target_url:$target_url}' > /tmp/qa-status.json

          curl --fail-with-body -L -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -d @/tmp/qa-status.json

          if test -f qa/latest-report.json; then
            INDEX=0
            while IFS= read -r ROW; do
              INDEX=$((INDEX + 1))
              NAME=$(printf '%s' "$ROW" | jq -r '.name' | cut -c1-70)
              DETAILS=$(printf '%s' "$ROW" | jq -r '.details // "Falha sem detalhes"' | tr '\n' ' ' | cut -c1-125)
              jq -n \
                --arg state "failure" \
                --arg context "qa/falha-${INDEX}" \
                --arg description "${NAME}: ${DETAILS}" \
                --arg target_url "$TARGET_URL" \
                '{state:$state,context:$context,description:$description,target_url:$target_url}' > /tmp/qa-failure.json
              curl --fail-with-body -L -X POST \
                -H "Accept: application/vnd.github+json" \
                -H "Authorization: Bearer ${GH_TOKEN}" \
                -H "X-GitHub-Api-Version: 2022-11-28" \
                "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
                -d @/tmp/qa-failure.json
            done < <(jq -c '.results[] | select(.status == "fail")' qa/latest-report.json)
          fi

      - name: Reprovar quando houver falhas
        if: steps.qa.outcome == 'failure'
        run: exit 1
